import EMRSplitView from "../../components/emr/EMRSplitView";

// "Sala" loads the same EMR Split View but pre-focused on the active patient
export default function SalaPage() {
  return <EMRSplitView />;
}
